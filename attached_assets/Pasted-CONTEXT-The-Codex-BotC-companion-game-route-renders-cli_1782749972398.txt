CONTEXT /
The Codex (BotC companion), /game route renders client/src/pages/Game.tsx. The player-tracker setup is the inline SetupWizard component in that file (lines ~73-518). Today its flow is: Step 1 player count (+ Storyteller/Player split), Step 2 names, Step 3 select script, then Start. There is no character preview of the chosen script.

This is Phase 4 Slice 4 of the Game/Reference unification. The shared, read-only character view already exists at client/src/components/character/ScriptView.tsx (By Team / By Night sort, First/Other night sub-toggle, all internal). The pure resolver exists at client/src/lib/script-resolve.ts (resolveScriptCharacters / countScriptCharacters). Reference already consumes ScriptView. Game does not yet.

TASK /
1. Reorder the SetupWizard player flow to script-first with a preview:
   (a) Select a Script  ->  (b) ScriptView preview of that script  ->  (c) player count  ->  (d) player names  ->  start game.
2. Keep the existing "All Characters" (no-script) option. If All Characters is chosen, skip or empty the preview (no script to preview) and continue.
3. On the preview step, render the SHARED ScriptView (read-only: characters resolved from the selected script, expand/collapse allowed, NO claim selection or game interaction). Resolve the character list with resolveScriptCharacters(selectedScript, { includeTravellers: false, includeFabled: false }) so the preview matches exactly what the in-game claim picker will later filter to (Game.tsx ~line 2725). Do NOT pass includeTravellers/includeFabled true; that is Reference's behavior, not Game's.
4. Preserve the onStart/createGame contract unchanged: still call onStart(playerCount, finalNames, scriptRef) with scriptRef = { id } | null. No change to use-player-game.ts.
5. Preserve all existing capabilities in the script step: official/community/custom lists, Create Custom Script (ScriptBuilderDialog), Scan Paper Script (which saves as custom and auto-selects). These must keep working after the reorder.
6. Preserve the Storyteller entry (stMode / STWizard) and ScriptBuilderDialog exactly. In your review, propose where the Storyteller/Player split should sit now that script selection is first, then WAIT for approval before building it.

DATA MODEL /
No schema changes. No change to GameScriptRef ({ id }) or PlayerGame. localStorage keys unchanged.

ACCESS /
Writable: client/src/pages/Game.tsx (SetupWizard only). You MAY read ScriptView.tsx, script-resolve.ts, night-order.ts, Reference.tsx for reference.
Do NOT modify: client/src/pages/GameSetup.tsx and client/src/pages/GameTracker.tsx (separate older DB flow, not the /game route). client/src/components/character/ScriptView.tsx (consume as-is; if it needs a prop change, STOP and report). use-player-game.ts. team-style.ts. game-data.ts.

CONSTRAINTS /
- Review against the live code first. List the exact files and the exact functions/JSX blocks you will touch, flag any disagreement with the plan above, then WAIT for my approval before writing code.
- Do NOT silently fix adjacent bugs or refactor anything outside the SetupWizard reorder. If you spot drift (e.g. the two pre-existing TS errors in F-8), REPORT it for follow-up, do not fix inline.
- Do NOT collapse the preview into a second copy of the card grid. Reuse the shared ScriptView component; if you find yourself duplicating its markup, stop, that is the wrong path.
- Read-only preview: do NOT wire claim-selection, trust, or any game mutation into the preview step.
- No em dashes in any user-facing copy (commas, periods, parentheses).
- Mobile-responsive; the preview must scroll cleanly on a phone (table-of-play use).
- data-testid required on every new interactive/display element, format: button-<action> for buttons, step-<name> for the preview step container, e.g. step-script-preview, button-preview-next, button-preview-back.

EXPECTED RESULT /
A reordered SetupWizard: pick a script (or All Characters), see a read-only shared ScriptView preview of that script's resolved roster, then set count, then names, then start. createGame receives the same { id } | null scriptRef as before. Scan-to-custom and Create Custom still land you back on the selected script. STWizard still reachable.

VERIFICATION CHECKLIST /
1. /game with no saved game shows the new script-first step order.
2. Selecting an official script shows a preview whose character set equals resolveScriptCharacters(script, {travellers:false, fabled:false}); spot-check Trouble Brewing count matches the script-step count label.
3. Preview uses the shared ScriptView (confirm import from components/character/ScriptView, not a new grid).
4. By Team / By Night toggle and First/Other night sub-toggle work inside the preview.
5. "All Characters" path: no broken preview, can proceed to count, starts a game with scriptRef null.
6. Custom script preview resolves by characterIds and shows no travellers/fabled.
7. Create Custom Script: build, save, returns to flow with the new script selected and previewable.
8. Scan Paper Script: still saves as custom and auto-selects (mock or real), preview reflects it.
9. Count step still enforces 5-20 and shows the team breakdown badges.
10. Names step still defaults Player N / Traveler N for >15.
11. Start: onStart called with (count, names, { id }) and the tracker opens with the script applied; in-game claim picker filters to the same roster the preview showed.
12. Storyteller path still opens STWizard and is unchanged.
13. Back navigation works at every new step without losing prior selections.
14. Existing ScriptView/night-order vitest suite still passes; add a test that the preview renders the resolved set for a known script.
15. Mobile: preview scrolls, sticky sort bar does not overlap the step nav.
16. No new TypeScript errors beyond the two pre-existing F-8 errors.
17. Read-only: confirm no claim/mutation handlers are wired into the preview.

OUT-OF-SCOPE FILE CHANGES /
List every file you changed. Flag any system file (.replit, .gitignore, configs) touched as a side effect. If GameSetup.tsx or GameTracker.tsx appear in your diff, you touched the wrong flow, revert.

Use Checkpoints after the reorder works and again after the preview works. End your completed-task summary with exactly:

Task: [one line]
Files changed: [list]
Verified: [how: built, loaded /game, ran vitest]
Deferred / out of scope found: [any, or none]
Pushed commit: pending