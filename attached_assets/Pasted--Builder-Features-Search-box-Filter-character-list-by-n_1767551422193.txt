### Builder Features:
- **Search box**: Filter character list by name
- **Grouped by team**: Townsfolk, Outsiders, Minions, Demons (collapsible sections)
- **Multi-select checkboxes**: Tap to toggle inclusion
- **Character count**: Show running total of selected characters
- **Team breakdown**: Show count per team
- **Edition badges**: Small label showing which edition each character is from (TB/BMR/S&V)
- **Quick select**: Optional "Select all Trouble Brewing" buttons for convenience

### Validation (optional):
- Warn if no Demon selected
- Warn if character count seems off for standard play
- Don't block saving - custom scripts can be experimental

## 5. APPLYING CUSTOM SCRIPT

### In Game Mode:
- Claims picker filters to custom script characters + Travelers
- Setup breakdown still uses standard player count rules
- Nominate/Vote flows unchanged

### In Reference:
- Character list filters to custom script characters + Travelers
- Can still switch to other filters (All, TB, BMR, S&V)
- Switching back to Custom shows the saved script

## 6. EDITING CUSTOM SCRIPT

### From Game Mode:
- Small "✏️ Edit" icon next to script selector when custom is active
- Opens builder with current selections pre-checked

### From Reference:
- "✏️ Edit Custom Script" button when custom filter is active
- Opens same builder

### Edits sync immediately to both views.

## 7. CLEARING CUSTOM SCRIPT

Add a "Clear Custom Script" option:
- In builder: "Clear All" + don't save
- In script selector: Option to "Remove Custom Script"
- Clearing removes it from both Game Mode and Reference

## 8. PERSISTENCE

Save custom script to localStorage:
- Key: 'clocktower-custom-script'
- Value: JSON of CustomScript interface
- Survives page refresh and browser sessions
- Single custom script at a time (for simplicity)

Future enhancement: Multiple saved scripts (not in this phase).

## 9. UI INDICATOR

When custom script is active, make it clear:

**Game Mode selector:**
"Custom: [Script Name] ✏️"

**Reference filter:**
"Custom: [Script Name] ✏️" or highlighted differently

## 10. TRAVELERS REMINDER

Travelers are ALWAYS included regardless of script selection (per previous fix).
Custom script characters + all Travelers = available characters.
