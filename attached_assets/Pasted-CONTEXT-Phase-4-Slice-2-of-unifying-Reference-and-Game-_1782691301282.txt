CONTEXT /
Phase 4, Slice 2 of unifying Reference and Game Mode. Move the CharacterCard component out of Reference.tsx into a shared file so the upcoming ScriptView (Slice 3) can render it. CharacterCard already composes the Phase 3 primitives (NightBadges, JinxBadge, JinxList, TeamBadge). This is a pure relocation: rendering and behavior (expand/collapse, animation) must stay byte-identical. Do not redesign the card.

TASK /
A. AUDIT-FIRST GATE (do this and report before changing anything). Inspect and report:
   - CharacterCard's current definition in Reference.tsx (props: char, isExpanded, onToggle; expand/collapse via framer-motion; the ability lead, the night/setup/jinx badges, the expanded Night Order / Reminder Tokens / Jinxes sections).
   - Every dependency it needs to function once moved: the team color helper (teamCard / getTeamColor), getJinxesForCharacter, the Phase 3 primitives, framer-motion, lucide icons, and any other local helper or constant it references in Reference.
   - Exactly what moves into the new file vs what stays in Reference. Confirm the move is byte-identical. Then WAIT for approval.
B. Create client/src/components/character/CharacterCard.tsx exporting CharacterCard with typed props. Move any card-only logic with it; for shared concerns use the existing shared modules (team-style, night-order, the character primitives) rather than re-importing Reference internals.
C. Reference.tsx imports CharacterCard from the new file and renders it exactly as before. Remove the now-inline definition.

DATA MODEL / No schema changes.
ACCESS / client/src/components/character/CharacterCard.tsx (new), client/src/pages/Reference.tsx. Nothing else.

CONSTRAINTS /
- Byte-identical: collapsed and expanded card render and behave exactly as today (ability lead, First Night / Other Nights / Setup / N Jinxes badges, team label, expanded Night Order, Reminder Tokens, Jinxes list, expand/collapse animation).
- Do NOT change layout, styling, props contract, or behavior.
- Do NOT touch Game.tsx or STWizard; ScriptView does not exist yet, do not build it here.
- Do NOT silently fix adjacent issues, including the 2 known pre-existing TS errors. Report under Other observations only.
- Dark gothic theme only. Preserve any existing data-testid on the card.

EXPECTED RESULT /
CharacterCard lives in client/src/components/character/CharacterCard.tsx and is imported by Reference; Reference renders identically.

VERIFICATION CHECKLIST /
1. CharacterCard.tsx exists with typed props and consumes the shared modules (team-style, night-order, primitives), not Reference internals.
2. No CharacterCard definition remains inline in Reference.tsx; it imports the shared one.
3. Reference cards render byte-identical collapsed: ability lead, night badges, setup badge, jinx badge, team label.
4. Reference cards render byte-identical expanded: Night Order, Reminder Tokens, Jinxes list.
5. Expand/collapse animation and toggle behavior unchanged.
6. tsc compiles, no new type errors (2 known pre-existing errors out of scope).
7. Any existing data-testid on the card preserved.
8. No file outside the 2 listed changed.

OUT-OF-SCOPE FILE CHANGES /
List every file changed. Expected: the 2 listed only. Flag any system file (.replit, .gitignore) touched as a side effect.

ON COMPLETION, end with exactly:

Task: [one line]
Files changed: [list]
Verified: [how: built, loaded page, ran test]
Deferred / out of scope found: [any, or none]
Pushed commit: [sha]