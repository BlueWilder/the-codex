CONTEXT /
Phase 4 of unifying Reference and Game Mode: build the shared ScriptView and rebuild Game's entry flow around it. This is the largest phase, so this is a read-only plan-mode audit to map the work and propose smallest-safe sub-slices before any build. Earlier phases already shipped and you should build on them, not redo them: team-style.ts (team colors), night-order.ts (getFirstNightChars/getOtherNightChars/nightOrderValue/compareEndTeams/compareSheetOrder), and client/src/components/character/ primitives (NightBadges, JinxBadge, JinxList, TeamBadge).

Target experience:
- Game Mode entry becomes "Select a Script": Base scripts, Saved custom scripts, or Scan a photo. Then it shows the ScriptView, then player count, then the live tracker (the tracker itself is unchanged).
- ScriptView = a script-scoped character view with a sort toggle (By Team = written order, or By Night order) that is sticky on mobile, the ability to see First Night and Other Nights together (two columns on desktop; stacked or per-card badges on mobile), and jinxes surfaced. Reference adopts this same ScriptView.
- The Storyteller Bag Builder (STWizard) is NOT retired in this phase; that is Phase 5. ScriptBuilderDialog stays (it is the custom-script editor / scan entry).

Decisions already made (carry these):
- Scanned scripts are SAVED as custom scripts (they get an id and appear in the saved list; auto-named, editable later).
- A started game stores a reference to the script by id and resolves character details (abilities, night order, jinxes) live from the one source. At game start it ALSO snapshots the list of character ids in play, so a later edit or delete of a custom script cannot change an in-progress game's roster, while details still resolve live.

TASK / (read-only assessment + design + sub-plan, report findings only)
A. Map Game's current setup flow (SetupWizard ~Game.tsx:76-169): its steps, how script selection works today (it embeds ScriptBuilderDialog and STWizard), how player count and names are collected, and the exact onStart(count, names, scriptRef) contract into usePlayerGame, plus the current GameScriptRef shape.
B. Map Reference's script-resolution logic (~Reference.tsx:315-347: official vs custom vs edition matching, TRAVELLER_SCRIPT_MAP, fabled inclusion, community scripts). Recommend how to extract it into a shared resolver (e.g., resolveScriptCharacters(scriptId)) that both Reference and ScriptView use. Flag every edge case it must preserve.
C. Design the shared ScriptView component: its props, how it consumes the existing helpers and primitives, the By Team / By Night sort toggle (sticky on mobile), the both-nights presentation (two columns desktop, mobile fallback), jinx surfacing, and where the lifted CharacterCard fits. Describe how Reference adopts it and how the new Game flow renders it.
D. Design the new Game entry flow (Select a Script -> ScriptView -> player count -> tracker) that replaces SetupWizard, preserving the onStart contract and keeping the two localStorage keys (clocktower_player_game, clocktower_st_session) separate. Show how scan-save-as-custom and the id-reference + roster-snapshot decisions wire in.
E. Identify risks and anything that could break (the tracker, usePlayerGame, persistence, resume, mobile).
F. Propose a phased sub-plan for Phase 4, smallest safe slices first (for example: extract the shared script resolver; build ScriptView and adopt it in Reference first as the lower-risk consumer; then the new Game Select-a-Script flow). Give the file list per sub-slice.
G. List open questions you need answered before the first sub-slice build prompt.

DATA MODEL / Note any persistence/shape changes you would propose (e.g., adding a character-id snapshot to the game state), but propose only; no changes in this task.
ACCESS / Read-only. Do NOT modify any code.
CONSTRAINTS /
- Plan-mode investigation only. Do NOT write or change code.
- Do NOT retire STWizard or ScriptBuilderDialog in this phase.
- Do NOT fix the 2 known pre-existing TS errors; report only.
- Build on the existing helpers and primitives; do not duplicate them.
- Stay within the question. List unrelated findings under "Other observations."

EXPECTED RESULT /
Report findings in this plan-mode response for Brad to paste back to his planning copilot. Cover A through G, specific with file paths and line numbers. End with your recommended first sub-slice and the open questions. Do NOT write findings to a file.