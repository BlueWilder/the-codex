GOAL
Redesign the Character Reference page (Reference.tsx) typography, spacing, and a few team colors, while keeping all existing behavior and content. This is the first slice of a Codex visual refresh. Reference page only.

SUGGESTION (the spec)
1. Headers to Fraunces. On the Reference page, the character name and the section headers (Night Order, Reminder Tokens, Flavor, How It Works, Tips & Tricks, Bluffing, How to Run, Jinxes, Setup) move from Cinzel Decorative to Fraunces, kept in the existing gold/amber. Do NOT change the global --font-display token; scope Fraunces to this page only (a page-level class or scoped style), so the rest of the app keeps Cinzel until we extend later.
2. Body stays Crimson Text, in the team color (unchanged behavior).
3. Spacing. Adopt a more generous rhythm: body line-height ~1.58, clear space between sections, and give the ability line a lead treatment (slightly larger, with a thin gold left-rule).
4. Team color changes in getTeamColor (lines ~24-31). Change ONLY these two:
   - traveler: from 'text-amber-400 border-amber-900/30 bg-amber-950/20' to a slate silver, e.g. 'text-slate-300 border-slate-600/40 bg-slate-800/20'.
   - fabled: from 'text-violet-400 border-violet-900/30 bg-violet-950/20' to a gold, e.g. 'text-[#efc344] border-[#efc344]/30 bg-[#efc344]/10'.
   Leave townsfolk, outsider, minion, demon, default exactly as they are.
5. Storyteller treatment for How to Run. The How to Run section is host-facing, so style it distinctly regardless of team: body text in amethyst (#c79fe6), inside a purple-tinted callout box (background ~#1b1626, border ~#3d2f57, rounded), with a small "Storyteller" tag and a wand icon in the header. This amethyst is fixed, not team-driven.
6. F-5 cleanup, do it here. Define the currently-undefined font CSS variables that tailwind.config.ts references: --font-sans, --font-serif, --font-mono. Suggested: --font-serif: 'Crimson Text', serif; --font-sans: 'Plus Jakarta Sans', system-ui, sans-serif (used for small labels, counts, pills); --font-mono: ui-monospace, monospace. Add Fraunces and Plus Jakarta Sans to the @import in client/src/index.css (Crimson Text and Cinzel Decorative already load).

ASSUMPTIONS I MADE AND WHAT I VERIFIED
- I cloned the repo and read client/src/pages/Reference.tsx. getTeamColor is at lines ~22-31 with the exact class strings above. Section labels and icons (Moon, Quote, Eye Lightbulb, Sword, etc.) are rendered inline in the CharacterCard component. The card is collapsible (isExpanded/onToggle) - keep that.
- Fonts load via the @import at the top of client/src/index.css. --font-display, --font-body, --font-handwriting are defined; --font-sans/serif/mono are referenced by tailwind.config.ts but never defined (this is the F-5 issue).
- The app is otherwise all-serif (Cinzel Decorative + Crimson Text). I am intentionally NOT touching other pages.

WHAT I COULD BE WRONG ABOUT / WATCH ITEMS
- On a Fabled card, the gold body text sits near the gold section headers. Eyeball it; if it reads muddy, we can darken the Fabled body slightly. Fabled cards are rare, acceptable for v1.
- Check the live workspace is current with GitHub before starting.

INSTRUCTION
Review this against the live code first. Confirm or correct my line references and the getTeamColor strings, flag any disagreement or risk, and list the exact files you will touch. Then WAIT for my approval before building. Do not build yet.

SCOPE GUARDRAILS
- Touch only client/src/pages/Reference.tsx and client/src/index.css (font @import + :root var definitions). 
- Do NOT change the global --font-display, and do NOT restyle other pages.
- Do NOT alter card content, the collapse/expand behavior, section order, or game-data.
- If you spot adjacent issues, report them separately, do not fix them inline.

ON COMPLETION
End your post-task summary with exactly this block:

Task: [one line]
Files changed: [list]
Verified: [how: built, loaded page, ran test]
Deferred / out of scope found: [any, or none]
Pushed commit: [sha]