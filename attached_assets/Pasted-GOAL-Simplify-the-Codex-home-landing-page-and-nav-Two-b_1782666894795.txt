GOAL
Simplify the Codex home landing page and nav. Two big cards come off the home page, one link comes out of the nav, and Game Mode becomes the home page's single primary action.

SUGGESTION (the spec)
1. client/src/components/Navigation.tsx: remove the "Game" item from the navItems array (the { href: "/game", icon: Gamepad2, label: "Game" } entry). Leave Guide (/introduction) and Reference (/reference) and the Sign in button as they are. Remove the now-unused Gamepad2 import from this file.
2. client/src/pages/Home.tsx: remove the two cards that link to /introduction ("Blood on the Clocktower") and /reference ("Character Reference"). They stay reachable via the Guide and Reference nav links, so nothing is lost.
3. Replace the three-column card grid with a single centered primary action button linking to /game, labeled "Enter Game Mode", with the Gamepad2 icon. Add one short muted supporting line under it: "Track players, claims, and votes during your live games."
4. Keep the "Codex" title and the flavor quote exactly as they are, including the existing motion/animation. Style the button as a prominent filled amber primary action (it can reuse the existing Button component or a styled Link, whichever is cleaner), distinct from the outline Sign in button.
5. Remove now-unused imports from Home.tsx (BookOpen, HelpCircle); keep Gamepad2.

ASSUMPTIONS I MADE AND WHAT I VERIFIED
- I read both files at commit 3b41613. Home.tsx renders three <Link> cards: /introduction, /reference, /game. Navigation.tsx navItems holds Guide (/introduction), Reference (/reference), Game (/game), plus the Sign in / user menu.
- The two removed cards map to nav links that remain (Guide, Reference), so removing the cards orphans nothing.
- /game has no other nav entry after this, so the home button becomes its main entry point. This is intended.

WHAT I COULD BE WRONG ABOUT / WATCH ITEMS
- After this, /game is reachable from the home page but not from the nav on other pages. Confirm that is the desired flow.
- Check the live workspace is current with GitHub before starting.

INSTRUCTION
Review this against the live code first. Confirm the file structure and imports, flag any disagreement or risk, and list the exact files you will touch. Then WAIT for my approval before building. Do not build yet.

SCOPE GUARDRAILS
- Touch only client/src/pages/Home.tsx and client/src/components/Navigation.tsx.
- Do NOT remove the /game route itself or the Game page. Do NOT change /introduction or /reference routes or pages.
- Keep the title, quote, and animations intact.
- If you spot adjacent issues, report them separately, do not fix them inline.

ON COMPLETION
End your post-task summary with exactly this block:

Task: [one line]
Files changed: [list]
Verified: [how: built, loaded page, ran test]
Deferred / out of scope found: [any, or none]
Pushed commit: [sha]