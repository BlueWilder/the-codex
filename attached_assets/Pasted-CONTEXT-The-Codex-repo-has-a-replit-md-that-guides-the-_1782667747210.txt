CONTEXT /
The Codex repo has a replit.md that guides the Replit agent. It is now stale: it lists Cinzel Decorative as the display font (we replaced it with Fraunces app-wide in commit 3b41613) and documents none of the new team-color or Storyteller design rules. Update it so future agent work stays consistent with the current design system and does not reach for generic or Path & Post defaults. Documentation only, no code changes.

TASK /
A. In the existing Typography line under Frontend Architecture, replace the Cinzel Decorative reference. New text:
   "Typography: Fraunces (display and all headings, replaced Cinzel Decorative app-wide 2026-06-28), Crimson Text (serif body), Plus Jakarta Sans (UI labels, counts, pills), Homemade Apple (handwriting accent). Font CSS variables in index.css: --font-display (Fraunces), --font-serif (Crimson Text), --font-sans (Plus Jakarta Sans), --font-mono (system mono), --font-handwriting (Homemade Apple)."

B. Add a new "### Design System" subsection under System Architecture with this content:
   - Theme: dark gothic only. There is NO light mode. Do not add light-mode styling or apply any external brand palette. The Codex has its own identity: charcoal background, parchment text, antique gold and amber accents, crimson primary.
   - Team color system: driven by getTeamColor in client/src/pages/Reference.tsx. Each team tints the card text, border, and background. Values: Townsfolk text-blue-400, Outsider text-blue-200, Minion text-red-400, Demon text-red-600, Traveler text-slate-300, Fabled text-[#efc344] (gold). Preserve this per-team tinting on any card surface.
   - Storyteller accent: the "How to Run" host instructions use a fixed amethyst (#c79fe6) regardless of team, inside a panel with border #3d2f57, a Wand2 icon, and a small "Storyteller" tag. Applied in Reference.tsx and STWizard.tsx. Keep host-facing content visually distinct from player-facing content.

C. Add one line under User Preferences: "No em dashes in user-facing copy. Use commas, periods, or parentheses."

DATA MODEL / No schema changes.
ACCESS / Write to replit.md only.
CONSTRAINTS /
- Modify ONLY replit.md. Do NOT touch any code, component, or style file.
- Do NOT remove unrelated existing sections; edit the Typography line in place and append the new Design System subsection.
- Do NOT apply any of these values to code in this task; this is documentation of what already exists.

EXPECTED RESULT /
- replit.md Typography line reflects Fraunces and the full font-variable list.
- A new Design System subsection documents the dark-gothic-only theme, the six team colors, and the amethyst Storyteller rule.
- A no-em-dash line under User Preferences.

VERIFICATION CHECKLIST /
1. replit.md no longer references Cinzel Decorative as the display font.
2. Fraunces is named as the display/heading font.
3. All six team colors are listed with their exact class values.
4. The amethyst (#c79fe6) Storyteller rule is present.
5. The dark-mode-only / no-external-brand note is present.
6. No file other than replit.md changed.

OUT-OF-SCOPE FILE CHANGES /
List every file changed. The expected list is replit.md only. Flag any system file (.replit, .gitignore) touched as a side effect.

INSTRUCTION /
Review against the live replit.md first, confirm the Typography line wording and section placement, then WAIT for my approval before writing. End your post-task summary with:

Task: [one line]
Files changed: [list]
Verified: [how: built, loaded page, ran test]
Deferred / out of scope found: [any, or none]
Pushed commit: [sha]