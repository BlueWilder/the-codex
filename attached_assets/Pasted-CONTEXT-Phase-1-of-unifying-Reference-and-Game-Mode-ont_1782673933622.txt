CONTEXT /
Phase 1 of unifying Reference and Game Mode onto shared code. Today the team colors are defined in 4 places with divergent values, and a recent change only updated Reference, so Game, the Storyteller wizard, and the script builder show stale/inconsistent colors. This task creates one team-color source and points all four at it. Decisions already made: Reference's values are canonical, Game's player surfaces will visibly change to match (intended), and the source must expose per-context variants, not one flat class string.

TASK /
A. AUDIT-FIRST GATE (do this and report before changing anything). Inspect and report:
   - The 4 current definitions and their exact class shapes: Reference.tsx getTeamColor (~:22-32, returns combined "text + border + bg"), Game.tsx TEAM_COLORS (~:31-38, "bg + text + border" badge style; used at ~:603, :894, :1105, :1320, :2275), STWizard.tsx TEAM_COLORS (~:13-18, badge style, only 4 teams), ScriptBuilderDialog.tsx TEAM_COLORS (~:21-28, "border + bg" only, no text).
   - The named-variant API you will expose, and the exact class string each of the 4 consumers will pull. Then WAIT for approval.
B. Create a single source (suggest client/src/lib/team-style.ts) covering all 6 teams (townsfolk, outsider, minion, demon, traveler, fabled) and exposing per-context named variants that cover the four shapes above. Canonical values from Reference.tsx getTeamColor:
   - townsfolk: text-blue-400, border-blue-900/30, bg-blue-950/20
   - outsider: text-blue-200, border-blue-800/30, bg-blue-900/10
   - minion: text-red-400, border-red-900/30, bg-red-950/20
   - demon: text-red-600, border-red-900/50, bg-red-950/30
   - traveler: text-slate-300, border-slate-600/40, bg-slate-800/20
   - fabled: text-[#efc344], border-[#efc344]/30, bg-[#efc344]/10
   For the badge variant (Game + STWizard), keep the existing badge treatment shape (bg-{hue}/60 text-{hue} border-{hue}) but in the canonical hue family, so outsider becomes blue not teal, minion red not orange, traveler slate not purple, fabled gold not violet.
C. Replace all 4 definitions to consume the source:
   - Reference.tsx getTeamColor: must render the SAME classes as today (no visual change to Reference).
   - Game.tsx (5 usages): switch to the badge/card variant; player surfaces adopt the canonical hues (expected visible change).
   - STWizard.tsx: consume the source; it gains traveler and fabled (previously missing).
   - ScriptBuilderDialog.tsx: consume the source's border+bg surface variant.
D. Keep per-context variants. Do NOT flatten to one class string. Each consumer pulls the variant it needs.

DATA MODEL / No schema changes.
ACCESS / client/src/lib/team-style.ts (new), client/src/pages/Reference.tsx, client/src/pages/Game.tsx, client/src/components/STWizard.tsx, client/src/components/ScriptBuilderDialog.tsx. Nothing else.

CONSTRAINTS /
- Do NOT change the canonical color VALUES. Reference must look identical after.
- Do NOT alter any layout, behavior, component logic, or unrelated styling.
- Do NOT silently fix adjacent issues found in these files. Report them under Other observations.
- Dark gothic theme only. Do NOT add light-mode styling.
- Do NOT leave any literal TEAM_COLORS or inline getTeamColor color map in the 4 files; all must reference the new source.

EXPECTED RESULT /
One team-style source; all 4 consumers reference it; Reference unchanged visually; Game/STWizard/ScriptBuilder now show the canonical scheme; STWizard colors traveler and fabled.

VERIFICATION CHECKLIST /
1. team-style.ts exports all 6 teams with named per-context variants.
2. No literal team-color map remains in Reference, Game, STWizard, or ScriptBuilderDialog.
3. Reference page renders identically: Townsfolk blue-400, Demon red-600, Traveler slate, Fabled gold.
4. Game Mode player surfaces now show outsider blue, minion red, traveler slate, fabled gold (changed from teal/orange/purple/violet). Check all 5 prior usages.
5. STWizard now colors traveler and fabled, and the 4 base teams are unchanged in look.
6. ScriptBuilderDialog border+bg still render correctly per team.
7. tsc compiles with no new type errors.
8. Load and eyeball each surface: Reference, Game setup, Game tracker, Storyteller wizard, Script builder.
9. No new interactive elements added, so no new data-testid needed (confirm none invented).
10. No file outside the 5 listed changed.

OUT-OF-SCOPE FILE CHANGES /
List every file changed. Expected: the 5 listed only. Flag any system file (.replit, .gitignore) touched as a side effect.

ON COMPLETION, end with exactly:

Task: [one line]
Files changed: [list]
Verified: [how: built, loaded page, ran test]
Deferred / out of scope found: [any, or none]
Pushed commit: [sha]