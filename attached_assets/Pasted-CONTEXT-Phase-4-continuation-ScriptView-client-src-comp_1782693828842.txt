CONTEXT /
Phase 4 continuation. ScriptView (client/src/components/character/ScriptView.tsx) currently renders a grid of CharacterCards; sorting still lives in the caller (Reference). Add a By Team / By Night sort toggle to ScriptView itself, sticky on mobile, and have Reference drive its sort through ScriptView. The goal is that when Game adopts ScriptView later, it inherits the toggle for free. Sort logic already exists in night-order.ts (nightOrderValue, compareEndTeams, compareSheetOrder). Reference already has sort controls today; this consolidates them into ScriptView.

TASK /
A. AUDIT-FIRST GATE (do this and report before changing anything). Inspect and report:
   - ScriptView's current props/API (it was built to accept optional controlled props) and how it currently receives its already-sorted character list.
   - Reference's current sort controls (the existing alphabetical / Night / Sheet controls and which icons/buttons drive them) and exactly how Reference sorts today using the night-order helpers.
   - Propose the cleanest integration: a controlled sortOrder ("team" | "night") plus onSortChange owned by ScriptView's toggle UI, with Reference passing/receiving it; or ScriptView owning sort state internally. Recommend one. Confirm the sorted output stays identical to Reference today for both modes. Then WAIT for approval.
B. Add the sort toggle to ScriptView: a two-option segmented control labeled "By Team" and "By Night", in a bar that is sticky on mobile (sticky top-0 z-10 within the page scroll, verified in the real page scroll, not a nested overflow container). "By Team" sorts via compareSheetOrder + compareEndTeams; "By Night" via nightOrderValue + compareEndTeams. Travelers and fabled stay at the end in both.
C. Adopt in Reference: Reference drives ScriptView's sort via the toggle, replacing its own inline sort control for these two modes. Preserve any existing Reference behavior the toggle does not cover (for example, if a separate "Sheet" compact view exists today, leave it as-is and report it; do not remove it in this task).

DATA MODEL / No schema changes.
ACCESS / client/src/components/character/ScriptView.tsx, client/src/pages/Reference.tsx. Add a small shared sort type if helpful. Nothing else.

CONSTRAINTS /
- Sorted output for "team" and "night" must match Reference's current ordering exactly (same helpers, travelers/fabled at end).
- Do NOT change CharacterCard, the primitives, the resolver, or Game/STWizard.
- Do NOT remove or alter any existing Reference view mode the toggle does not replace; report it instead.
- Do NOT silently fix adjacent issues, including the 2 known pre-existing TS errors. Report under Other observations only.
- Dark gothic theme only. New interactive elements get data-testid (e.g., button-sort-team, button-sort-night).

EXPECTED RESULT /
ScriptView owns a sticky By Team / By Night toggle; Reference drives sort through it; ordering is unchanged from today; the toggle is ready for Game to inherit.

VERIFICATION CHECKLIST /
1. ScriptView renders a sticky By Team / By Night toggle with data-testids.
2. "By Team" matches Reference's current team/sheet ordering exactly (spot-check a script).
3. "By Night" matches Reference's current night ordering exactly, travelers/fabled at end.
4. The toggle is sticky in the actual page scroll on a narrow viewport.
5. Reference no longer hand-rolls the team/night sort it delegated to ScriptView; any other view mode it had is untouched and reported.
6. CharacterCard, primitives, resolver, Game, STWizard untouched.
7. tsc compiles, no new errors (2 known pre-existing errors out of scope).
8. No file outside scope changed.

OUT-OF-SCOPE FILE CHANGES /
List every file changed. Flag any system file (.replit, .gitignore) touched as a side effect.

ON COMPLETION, end with exactly:

Task: [one line]
Files changed: [list]
Verified: [how: built, loaded page, ran test]
Deferred / out of scope found: [any, or none]
Pushed commit: [sha]