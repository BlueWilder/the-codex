GOAL
Remove dead-weight web fonts from the Codex to lighten the font payload on mobile (the app is used on a phone at the table). No visual change intended.

SUGGESTION
In client/index.html only, delete the single <link rel="stylesheet"> tag that loads fonts from fonts.googleapis.com. Its href lists ~30 families (Architects Daughter, DM Sans, Fira Code, Geist, Geist Mono, IBM Plex Sans/Mono, Inter, JetBrains Mono, Libre Baskerville, Lora, Merriweather, Montserrat, Open Sans, Outfit, Oxanium, Playfair Display, Plus Jakarta Sans, Poppins, Roboto, Roboto Mono, Source Code Pro, Source Serif 4, Space Grotesk, Space Mono). Keep the two <link rel="preconnect"> lines above it.

ASSUMPTIONS I MADE AND WHAT I VERIFIED
- I cloned the public repo at commit 405fc98 and grepped client/src and tailwind.config.ts for every one of those ~30 families. All returned zero references. They are not used anywhere.
- The three fonts the app actually uses (Cinzel Decorative, Crimson Text, Homemade Apple) load via the @import at the top of client/src/index.css, which this change does NOT touch.
- I verified Tailwind defines font-sans / font-serif / font-mono against CSS vars --font-sans / --font-serif / --font-mono that are never defined. font-serif is used widely and already falls back to the inherited Crimson Text body font today. Deleting this link does not change that, because none of the ~30 families were ever wired to those vars.
- Net: this is dead-code removal, the app should render identically.

WHAT I COULD BE WRONG ABOUT
Check against the live workspace, which may be ahead of commit 405fc98. If any of those families IS referenced in current code (a recent inline font-family, a new component, or a built mockup that ships to production), flag it before removing.

INSTRUCTION
Review this against the live code first. Confirm or correct my verification, flag any disagreement or risk, and list the exact files you will touch. Then WAIT for my approval before building. Do not build yet.

SCOPE GUARDRAILS
- Touch only client/index.html. Do NOT modify client/src/index.css.
- Do NOT add or remove any other fonts.
- If you spot adjacent issues, report them separately, do not fix them inline.

ON COMPLETION
End your post-task summary with exactly this block:

Task: [one line]
Files changed: [list]
Verified: [how: built, loaded page, ran test]
Deferred / out of scope found: [any, or none]
Pushed commit: [sha]